# SIP PR-risk engine — smoke test

This file is added by a throwaway PR to give the SIP code-intelligence
PR-risk engine a real diff to analyze (deterministic risk + blast radius +
security/OSV + a review comment). It changes no application code and is safe
to close or delete.

- Engine: deterministic-first PR-risk (LoRA explains, never decides)
- Target repo: bharat-os (JS/TS)
- Opened for end-to-end prod testing.
